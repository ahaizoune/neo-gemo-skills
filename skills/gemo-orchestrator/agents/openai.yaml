interface:
  display_name: "Gemo Orchestrator"
  short_description: "Brainstorm, groom, and orchestrate Gemo delivery"
  default_prompt: "Use $gemo-orchestrator to choose the right mode for this Gemo feature, start in brainstorm mode when the scope is still forming, use Figma MCP if design input is provided, and produce the right packet for the current phase."

policy:
  allow_implicit_invocation: true
