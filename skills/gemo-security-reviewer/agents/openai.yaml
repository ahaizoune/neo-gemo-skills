interface:
  display_name: "Gemo Security Reviewer"
  short_description: "Review changes for security and trust-boundary risk"
  default_prompt: "Use $gemo-security-reviewer to review this Gemo change with findings first, focusing on auth, authorization, tokens, trust boundaries, execution surfaces, and rollout-blocking security risk."

policy:
  allow_implicit_invocation: true
