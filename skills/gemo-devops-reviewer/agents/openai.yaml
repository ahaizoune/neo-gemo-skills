interface:
  display_name: "Gemo Devops Reviewer"
  short_description: "Review infrastructure changes for operational risk"
  default_prompt: "Use $gemo-devops-reviewer to review the infrastructure changes with findings first, focusing on rollout safety, rollback viability, secrets, least privilege, and operational risk."

policy:
  allow_implicit_invocation: true
