interface:
  display_name: "Gemo Backend Reviewer"
  short_description: "Detailed backend review with proactive risk prevention"
  default_prompt: "Use $gemo-backend-reviewer to review the backend changes with findings first, providing detailed stack-aware feedback, root-cause analysis, safer remediation guidance, and proactive prevention advice across FastAPI, Starlette, SlowAPI, SQLAlchemy 2 async, PostgreSQL, Alembic, Pydantic v2, Redis/RQ, httpx integrations, auth and token boundaries, observability, state-machine integrity, and rollout safety."

policy:
  allow_implicit_invocation: true
