interface:
  display_name: "Gemo Extension Reviewer"
  short_description: "Review extension changes for runtime safety"
  default_prompt: "Use $gemo-extension-reviewer to review the extension changes for message safety, permissions, and runtime correctness."

policy:
  allow_implicit_invocation: true
